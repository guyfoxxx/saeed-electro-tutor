import { route } from './router'; export interface Env{AI:Ai;TELEGRAM_BOT_TOKEN:string;OPENAI_KEY?:string;HF_TOKEN?:string;OPENROUTER_KEY?:string;CF_AI_BASE?:string;ADMIN_IDS?:string;USER_DB:KVNamespace;QBANK_DB:KVNamespace;ENV?:string;} export default{{async fetch(request:Request,env:Env,ctx:ExecutionContext){try{return await route(request,env,ctx);}catch(e){return new Response(`Error: ${(e as Error).message}`,{status:500});}}}};\n